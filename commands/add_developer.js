module.exports = {
	name: 'addDev',
    description: '[Developer only] : adds a new developer by id',
	execute(message) {
        const Discord = require('discord.js');
        const devs = require('../devs.json')
        const fs = require('fs');
            
	}
};